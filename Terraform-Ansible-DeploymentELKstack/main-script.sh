# terraform commands
cd ./terraform/
terraform init
terraform validate
terraform plan
#terraform apply


#once the terraform deployment is complete, use the below part

#sudo ansible-playbook .\ansible\elasticsearch\elasticsearch.yml

